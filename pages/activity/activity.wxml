<view class="page">
  <view class="container">
    <view class="admin-bar" wx:if="{{isAdmin}}">
      <text class="admin-text">管理员模式</text>
      <view class="admin-btn" bindtap="onAddActivity"><text>+ 新增活动</text></view>
    </view>
    <view class="subscribe-bar" wx:if="{{subscribeConfig.activity}}">
      <view class="sub-info">
        <text class="sub-title">活动更新通知</text>
        <text class="sub-desc">订阅获取最新活动信息提醒</text>
      </view>
      <view class="sub-btn {{subscribedActivity ? 'active' : ''}}" bindtap="subscribeActivity">
        {{subscribedActivity ? '已订阅(' + subscribeCount + ')' : '+ 订阅通知'}}
      </view>
    </view>
    <view class="tabs">
      <view class="tab {{status===item?'on':''}}" wx:for="{{statuses}}" wx:key="*this" bindtap="onStatus" data-s="{{item}}">{{item}}</view>
    </view>
    <view class="act-list">
      <view class="act-card headline" wx:for="{{filtered}}" wx:key="id" wx:if="{{item.status==='置顶'}}">
        <view class="act-header" bindtap="onExpand" data-id="{{item.id}}">
          <view class="act-badges">
            <view class="badge badge-top">📌 置顶头条</view>
            <view class="badge badge-official" wx:if="{{item.type==='官方权威信息'}}">官方权威信息</view>
          </view>
          <text class="act-title headline-title">{{item.title}}</text>
          <view class="act-summary" wx:if="{{item.content && (!item.richContent || item.richContent.length === 0)}}"><rich-text nodes="{{item.content}}" /></view>
          <block wx:if="{{item.richContent && item.richContent.length > 0}}">
            <view class="act-rich-content">
              <block wx:for="{{item.richContent}}" wx:for-item="block" wx:key="index">
                <view wx:if="{{block.type === 'music'}}" class="rich-music-block {{currentPlayUrl === block.url && isPlaying ? 'playing' : ''}}" catchtap="playSong" data-url="{{block.url}}" data-name="{{block.name || '推荐单曲'}}" data-cover="{{block.cover || ''}}">
                  <image lazy-load wx:if="{{block.cover}}" class="music-cover" src="{{block.cover}}" mode="aspectFill" />
                  <text class="music-icon {{currentPlayUrl === block.url && isPlaying ? 'rotating' : ''}}">🎵</text>
                  <view class="music-info">
                    <text class="music-name">{{block.name || '推荐单曲'}}</text>
                    <text class="music-url">{{block.url}}</text>
                  </view>
                  <text class="music-play-btn">{{currentPlayUrl === block.url && isPlaying ? '暂停' : '点我听歌'}}</text>
                </view>
                <view wx:elif="{{block.type === 'iframe'}}" class="rich-iframe-block">
                  <text class="iframe-icon">🌐</text>
                  <view class="iframe-info">
                    <text class="iframe-name">{{block.name || '外部网页'}}</text>
                    <text class="iframe-url">{{block.url}}</text>
                  </view>
                  <view class="iframe-actions">
                    <view class="iframe-btn open" catchtap="openIframe" data-url="{{block.url}}">打开</view>
                    <view class="iframe-btn copy" catchtap="copyIframe" data-url="{{block.url}}">复制</view>
                  </view>
                </view>
                <view wx:elif="{{block.type === 'quote'}}" class="rich-quote-block">
                  <text class="quote-text">{{block.content}}</text>
                </view>
                <view wx:elif="{{block.type === 'video'}}" class="rich-music-block" catchtap="openVideo" data-url="{{block.url}}" data-name="{{block.name || '视频'}}">
                  <text class="music-icon">
                    <block wx:if="{{block.platform === 'bilibili'}}">📺</block>
                    <block wx:elif="{{block.platform === 'weibo'}}">🎬</block>
                    <block wx:elif="{{block.platform === 'douyin'}}">🎵</block>
                    <block wx:elif="{{block.platform === 'kuaishou'}}">📹</block>
                    <block wx:elif="{{block.platform === 'tencent'}}">📽️</block>
                    <block wx:elif="{{block.platform === 'xiaohongshu'}}">📕</block>
                    <block wx:elif="{{block.platform === 'youtube'}}">🔴</block>
                    <block wx:else>🎥</block>
                  </text>
                  <view class="music-info">
                    <text class="music-name">{{block.name || block.platformName + '视频'}}</text>
                    <text class="music-url">{{block.url}}</text>
                  </view>
                  <text class="music-play-btn">▶ 观看</text>
                </view>
                <view wx:elif="{{block.type === 'table'}}" class="rich-table-block">
                  <view class="rich-table">
                    <view class="rich-table-row rich-table-header">
                      <view class="rich-table-cell" wx:for="{{block.headers}}" wx:key="index">{{item}}</view>
                    </view>
                    <view class="rich-table-row" wx:for="{{block.rows}}" wx:for-item="row" wx:key="index">
                      <view class="rich-table-cell" wx:for="{{row}}" wx:for-item="cell" wx:key="index">{{cell}}</view>
                    </view>
                  </view>
                </view>
                <view wx:elif="{{block.type === 'image'}}" class="rich-image-block" catchtap="previewRichImage" data-src="{{block.url}}">
                  <image lazy-load class="rich-image" src="{{block.url}}" mode="widthFix" />
                </view>
                <view wx:elif="{{block.type === 'divider'}}" class="divider-block">
                  <view class="divider-line {{block.style}}"></view>
                </view>
                <view wx:elif="{{block.type === 'link' && block.isVideo}}" class="rich-link-block video-link" catchtap="openVideo" data-url="{{block.url}}" data-name="{{block.text}}">
                  <text class="link-icon">📺</text>
                  <text class="link-text">{{block.text}}</text>
                  <text class="link-badge">点击观看</text>
                </view>
                <view wx:elif="{{block.type === 'link'}}" class="rich-link-block" catchtap="openLink" data-url="{{block.url}}" data-text="{{block.text}}">
                  <text class="link-icon">🔗</text>
                  <text class="link-text">{{block.text}}</text>
                  <text class="link-badge">点击复制</text>
                </view>
                <view wx:elif="{{block.type === 'attachment'}}" class="rich-link-block attachment-link" catchtap="downloadAttachment" data-url="{{block.url}}" data-name="{{block.name}}" data-size="{{block.sizeText}}">
                  <text class="link-icon">📎</text>
                  <text class="link-text">{{block.name}} ({{block.sizeText}})</text>
                  <text class="link-badge">下载预览</text>
                </view>
                <view wx:else class="rich-text-block" style="font-size:{{block.size}}rpx;color:{{block.color}};font-weight:{{block.weight || 'normal'}};font-style:{{block.style || 'normal'}};{{block.fontFamily && block.fontFamily !== 'sans-serif' ? 'font-family:' + block.fontFamily + ';' : ''}}">
                  <rich-text nodes="{{block.html || block.content}}" />
                </view>
              </block>
            </view>
          </block>
          <view class="act-summary" wx:else><rich-text nodes="{{item.desc}}" /></view>
        </view>
        <view class="act-source" wx:if="{{item.source}}">来源：{{item.source}}</view>
        <view class="act-footer" wx:if="{{item.rewards.length > 0 || item.start}}">
          <view class="act-rewards" wx:if="{{item.rewards.length > 0}}">
            <text class="act-reward" wx:for="{{item.rewards}}" wx:for-item="r" wx:key="*this">{{r}}</text>
          </view>
          <text class="act-date" wx:if="{{item.start}}">{{item.start}} {{item.startTime || ''}}~{{item.end}} {{item.endTime || ''}}</text>
        </view>
        <view class="act-admin-btns" wx:if="{{isAdmin}}">
          <view class="act-admin-btn edit" catchtap="onEditActivity" data-item="{{item}}">编辑</view>
          <view class="act-admin-btn del" catchtap="onDeleteActivity" data-item="{{item}}" wx:if="{{item.isCloud}}">删除</view>
        </view>
      </view>
    </view>
    <view class="section-title" wx:if="{{status==='全部'}}">
      <text class="section-line"></text>
      <text class="section-text">其他活动</text>
      <text class="section-line"></text>
    </view>
    <view class="act-list">
      <view class="act-card" wx:for="{{filtered}}" wx:key="id" wx:if="{{item.status!=='置顶'}}">
        <view class="act-badges">
          <view class="badge badge-go" wx:if="{{item.status==='进行中'}}">进行中</view>
          <view class="badge badge-soon" wx:if="{{item.status==='即将开始'}}">即将开始</view>
          <view class="badge badge-type" wx:if="{{item.type}}">{{item.type}}</view>
        </view>
        <text class="act-title">{{item.title}}</text>
        <image lazy-load class="act-image" wx:if="{{item.image}}" src="{{item.image}}" mode="widthFix" />
        <view class="act-desc" wx:if="{{item.content && (!item.richContent || item.richContent.length === 0)}}"><rich-text nodes="{{item.content}}" /></view>
        <block wx:if="{{item.richContent && item.richContent.length > 0}}">
          <view class="act-rich-content">
            <block wx:for="{{item.richContent}}" wx:for-item="block" wx:key="index">
              <view wx:if="{{block.type === 'music'}}" class="rich-music-block {{currentPlayUrl === block.url && isPlaying ? 'playing' : ''}}" catchtap="playSong" data-url="{{block.url}}" data-name="{{block.name || '推荐单曲'}}">
                <text class="music-icon {{currentPlayUrl === block.url && isPlaying ? 'rotating' : ''}}">🎵</text>
                <view class="music-info">
                  <text class="music-name">{{block.name || '推荐单曲'}}</text>
                  <text class="music-url">{{block.url}}</text>
                </view>
                <text class="music-play-btn">{{currentPlayUrl === block.url && isPlaying ? '暂停' : '点我听歌'}}</text>
              </view>
              <view wx:elif="{{block.type === 'iframe'}}" class="rich-iframe-block">
                <text class="iframe-icon">🌐</text>
                <view class="iframe-info">
                  <text class="iframe-name">{{block.name || '外部网页'}}</text>
                  <text class="iframe-url">{{block.url}}</text>
                </view>
                <view class="iframe-actions">
                  <view class="iframe-btn open" catchtap="openIframe" data-url="{{block.url}}">打开</view>
                  <view class="iframe-btn copy" catchtap="copyIframe" data-url="{{block.url}}">复制</view>
                </view>
              </view>
              <view wx:elif="{{block.type === 'quote'}}" class="rich-quote-block">
                <text class="quote-text">{{block.content}}</text>
              </view>
              <view wx:elif="{{block.type === 'video' && block.isDirectMp4}}" class="rich-video-inline">
                <video class="video-player-video" src="{{block.url}}" controls show-center-play-btn object-fit="contain" style="width:100%; border-radius:12rpx; margin-top:10rpx;" />
              </view>
              <view wx:elif="{{block.type === 'video'}}" class="rich-music-block" catchtap="openVideo" data-url="{{block.url}}" data-name="{{block.name || '视频'}}">
                <text class="music-icon">
                  <block wx:if="{{block.platform === 'bilibili'}}">📺</block>
                  <block wx:elif="{{block.platform === 'weibo'}}">🎬</block>
                  <block wx:elif="{{block.platform === 'douyin'}}">🎵</block>
                  <block wx:elif="{{block.platform === 'kuaishou'}}">📹</block>
                  <block wx:elif="{{block.platform === 'tencent'}}">📽️</block>
                  <block wx:elif="{{block.platform === 'xiaohongshu'}}">📕</block>
                  <block wx:elif="{{block.platform === 'youtube'}}">🔴</block>
                  <block wx:else>🎥</block>
                </text>
                <view class="music-info">
                  <text class="music-name">{{block.name || block.platformName + '视频'}}</text>
                  <text class="music-url">{{block.url}}</text>
                </view>
                <text class="music-play-btn">▶ 观看</text>
              </view>
              <view wx:elif="{{block.type === 'table'}}" class="rich-table-block">
                <view class="rich-table">
                  <view class="rich-table-row rich-table-header">
                    <view class="rich-table-cell" wx:for="{{block.headers}}" wx:key="index">{{item}}</view>
                  </view>
                  <view class="rich-table-row" wx:for="{{block.rows}}" wx:for-item="row" wx:key="index">
                    <view class="rich-table-cell" wx:for="{{row}}" wx:for-item="cell" wx:key="index">{{cell}}</view>
                  </view>
                </view>
              </view>
              <view wx:elif="{{block.type === 'image'}}" class="rich-image-block" catchtap="previewRichImage" data-src="{{block.url}}">
                <image lazy-load class="rich-image" src="{{block.url}}" mode="widthFix" />
              </view>
              <view wx:elif="{{block.type === 'divider'}}" class="divider-block">
                <view class="divider-line {{block.style}}"></view>
              </view>
              <view wx:elif="{{block.type === 'link' && block.isVideo}}" class="rich-link-block video-link" catchtap="openVideo" data-url="{{block.url}}" data-name="{{block.text}}">
                <text class="link-icon">📺</text>
                <text class="link-text">{{block.text}}</text>
                <text class="link-badge">点击观看</text>
              </view>
              <view wx:elif="{{block.type === 'link'}}" class="rich-link-block" catchtap="openLink" data-url="{{block.url}}" data-text="{{block.text}}">
                <text class="link-icon">🔗</text>
                <text class="link-text">{{block.text}}</text>
                <text class="link-badge">点击复制</text>
              </view>
              <view wx:elif="{{block.type === 'attachment'}}" class="rich-link-block attachment-link" catchtap="downloadAttachment" data-url="{{block.url}}" data-name="{{block.name}}" data-size="{{block.sizeText}}">
                <text class="link-icon">📎</text>
                <text class="link-text">{{block.name}} ({{block.sizeText}})</text>
                <text class="link-badge">下载预览</text>
              </view>
              <view wx:else class="rich-text-block" style="font-size:{{block.size}}rpx;color:{{block.color}};font-weight:{{block.weight || 'normal'}};font-style:{{block.style || 'normal'}};{{block.fontFamily && block.fontFamily !== 'sans-serif' ? 'font-family:' + block.fontFamily + ';' : ''}}">
                <rich-text nodes="{{block.html || block.content}}" />
              </view>
            </block>
          </view>
        </block>
        <view class="act-desc" wx:else><rich-text nodes="{{item.desc}}" /></view>
        <view class="act-source" wx:if="{{item.source}}">来源：{{item.source}}</view>
        <view class="act-footer" wx:if="{{item.rewards.length > 0 || item.start}}">
          <view class="act-rewards" wx:if="{{item.rewards.length > 0}}">
            <text class="act-reward" wx:for="{{item.rewards}}" wx:for-item="r" wx:key="*this">{{r}}</text>
          </view>
          <text class="act-date" wx:if="{{item.start}}">{{item.start}} {{item.startTime || ''}}~{{item.end}} {{item.endTime || ''}}</text>
        </view>
        <view class="act-admin-btns" wx:if="{{isAdmin}}">
          <view class="act-admin-btn edit" catchtap="onEditActivity" data-item="{{item}}">编辑</view>
          <view class="act-admin-btn del" catchtap="onDeleteActivity" data-item="{{item}}" wx:if="{{item.isCloud}}">删除</view>
        </view>
      </view>
    </view>
    <view class="empty" wx:if="{{filtered.length===0}}">暂无活动</view>
  </view>

  <!-- 编辑弹窗 -->
  <view class="modal-mask" wx:if="{{showEditModal}}" bindtap="onCloseModal">
    <view class="modal-box" catchtap="preventClose">
      <view class="modal-header">
        <text class="modal-title">{{editingItem ? '编辑活动' : '新增活动'}}</text>
        <text class="modal-close" bindtap="onCloseModal">✕</text>
      </view>
      <view class="modal-body">
        <view class="form-group">
          <text class="form-label">标题</text>
          <input class="form-input" placeholder="活动标题" value="{{editTitle}}" bindinput="onEditInput" maxlength="50" />
        </view>
        <view class="form-group">
          <text class="form-label">类型</text>
          <input class="form-input" placeholder="如：限时活动、日常活动" value="{{editType}}" bindinput="onTypeInput" />
        </view>
        <view class="form-group">
          <text class="form-label">状态</text>
          <picker class="form-picker" range="{{['进行中','即将开始','置顶']}}" bindchange="onStatusChange">
            <text class="picker-text">{{editStatus}} ▾</text>
          </picker>
        </view>
        <view class="form-group">
          <text class="form-label">描述</text>
          
          <view class="rich-toolbar" style="margin-bottom: 20rpx; background: rgba(0,0,0,0.2); padding: 10rpx; border-radius: 12rpx; display: flex; flex-wrap: wrap; gap: 10rpx;">
            <view class="tool-btn" catchtap="showMusicDialog" style="padding: 10rpx; background: rgba(255,255,255,0.05); border-radius: 8rpx; display: flex; align-items: center; gap: 6rpx;">
              <text class="tool-icon">🎵</text><text class="tool-label" style="font-size: 24rpx; color: #ccc;">音乐</text>
            </view>
            <view class="tool-btn" catchtap="showVideoDialog" style="padding: 10rpx; background: rgba(255,255,255,0.05); border-radius: 8rpx; display: flex; align-items: center; gap: 6rpx;">
              <text class="tool-icon">🎬</text><text class="tool-label" style="font-size: 24rpx; color: #ccc;">视频</text>
            </view>
            <view class="tool-btn" catchtap="showImageBlockDialog" style="padding: 10rpx; background: rgba(255,255,255,0.05); border-radius: 8rpx; display: flex; align-items: center; gap: 6rpx;">
              <text class="tool-icon">🖼</text><text class="tool-label" style="font-size: 24rpx; color: #ccc;">插图</text>
            </view>
          </view>

          <!-- 富文本预览区 -->
          <view class="rich-blocks" wx:if="{{editRichContent && editRichContent.length > 0}}" style="margin-bottom: 20rpx; display: flex; flex-direction: column; gap: 16rpx;">
            <view class="rich-block" wx:for="{{editRichContent}}" wx:key="index" style="background: rgba(255,255,255,0.05); padding: 16rpx; border-radius: 12rpx; position: relative;">
              <block wx:if="{{item.type === 'music'}}">
                <text>🎵 {{item.name || '音乐'}} ({{item.url}})</text>
              </block>
              <block wx:elif="{{item.type === 'video'}}">
                <text>🎬 {{item.name || '视频'}} ({{item.url}})</text>
              </block>
              <block wx:elif="{{item.type === 'image'}}">
                <image lazy-load src="{{item.url}}" mode="aspectFill" style="width: 100%; height: 200rpx; border-radius: 8rpx;" />
              </block>
              <view class="block-delete" catchtap="removeRichBlock" data-idx="{{index}}" style="position: absolute; right: 10rpx; top: 10rpx; background: rgba(255,0,0,0.5); padding: 4rpx 12rpx; border-radius: 8rpx; font-size: 20rpx;">删除</view>
            </view>
          </view>

          <textarea class="form-textarea" placeholder="活动描述..." value="{{editDesc}}" bindinput="onDescInput" maxlength="500" cursor-spacing="120" />
          <view class="file-import" catchtap="importActivityFile">
            <text class="file-import-icon">📄</text>
            <text class="file-import-text">导入文件（XML/DOCX）</text>
          </view>
        </view>
        <view class="form-group">
          <text class="form-label">奖励（每行一个）</text>
          <textarea class="form-textarea" placeholder="洛克贝x1000&#10;经验果x5" value="{{editRewards}}" bindinput="onRewardsInput" maxlength="200" cursor-spacing="120" />
        </view>
        <view class="form-row">
          <view class="form-group flex-3">
            <text class="form-label">开始日期</text>
            <picker mode="date" value="{{editStart}}" bindchange="onStartDateChange">
              <view class="form-picker-value {{editStart ? '' : 'placeholder'}}">{{editStart || '选择日期'}}</view>
            </picker>
          </view>
          <view class="form-group flex-2">
            <text class="form-label">开始时间</text>
            <picker mode="time" value="{{editStartTime}}" bindchange="onStartTimeChange">
              <view class="form-picker-value {{editStartTime ? '' : 'placeholder'}}">{{editStartTime || '时间'}}</view>
            </picker>
          </view>
        </view>
        <view class="form-row">
          <view class="form-group flex-3">
            <text class="form-label">结束日期</text>
            <picker mode="date" value="{{editEnd}}" bindchange="onEndDateChange">
              <view class="form-picker-value {{editEnd ? '' : 'placeholder'}}">{{editEnd || '选择日期'}}</view>
            </picker>
          </view>
          <view class="form-group flex-2">
            <text class="form-label">结束时间</text>
            <picker mode="time" value="{{editEndTime}}" bindchange="onEndTimeChange">
              <view class="form-picker-value {{editEndTime ? '' : 'placeholder'}}">{{editEndTime || '时间'}}</view>
            </picker>
          </view>
        </view>
        <view class="form-group">
          <text class="form-label">封面图（可选）</text>
          <view class="image-section">
            <view class="image-preview" wx:if="{{editImage}}" catchtap="previewEditImage">
              <image lazy-load class="preview-img" src="{{editImage}}" mode="aspectFill" />
              <view class="image-remove" catchtap="removeEditImage">✕</view>
            </view>
            <view class="image-actions" wx:if="{{!editImage}}">
              <view class="image-action-btn" catchtap="chooseEditImage">
                <text class="action-icon">📷</text>
                <text class="action-text">相册</text>
              </view>
              <view class="image-action-btn" catchtap="inputEditImageUrl">
                <text class="action-icon">🔗</text>
                <text class="action-text">链接</text>
              </view>
            </view>
          </view>
        </view>
      </view>
      <view class="modal-footer">
        <button class="modal-submit" bindtap="onSubmitActivity" disabled="{{submitting}}">{{submitting ? '提交中...' : (editingItem ? '更新' : '发布')}}</button>
      </view>
    </view>
  </view>

  <!-- 视频播放器 -->
  <view class="modal-mask" wx:if="{{showVideoPlayer}}" catchtap="closeVideoPlayer" style="z-index:10000;">
    <view class="modal-box video-player-box" catchtap="preventClose" style="width:100%;background:rgba(10,14,39,0.98);border-radius:32rpx 32rpx 0 0;padding:0 0 constant(safe-area-inset-bottom) 0;padding:0 0 env(safe-area-inset-bottom) 0;max-height:90vh;box-shadow:0 -8rpx 40rpx rgba(0,0,0,0.5);">
      <view class="modal-header" style="padding:32rpx 32rpx 0;margin-bottom:12rpx;">
        <text class="modal-title" style="color:#00d4ff;font-size:30rpx;">{{videoPlayerName}}</text>
        <text class="modal-close" catchtap="closeVideoPlayer">✕</text>
      </view>
      <view style="padding:0 32rpx 12rpx;display:flex;align-items:center;gap:12rpx;" wx:if="{{videoPlayerOwner}}">
        <view style="width:4rpx;height:24rpx;background:#00d4ff;border-radius:2rpx;"></view>
        <text style="font-size:24rpx;color:rgba(255,255,255,0.5);">UP主: {{videoPlayerOwner}}</text>
      </view>
      <video style="width:100%;height:420rpx;display:block;" src="{{videoPlayerUrl}}" controls autoplay show-center-play-btn object-fit="contain" />
      <scroll-view scroll-y class="video-player-desc" wx:if="{{videoPlayerDesc}}">
        <text class="video-player-desc-text" space="ensp" decode="true">{{videoPlayerDesc}}</text>
      </scroll-view>
    </view>
  </view>

  <dock current="" />
</view>
